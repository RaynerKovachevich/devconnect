# Copy this file to .env before running the app
# Values shown here are safe defaults for local development.

# Express server port (matches src/app.ts default)
PORT=5001

# Postgres connection string used by Prisma
# Update host/user/password if you run Postgres elsewhere.
DATABASE_URL="postgresql://devconnect_user:devconnect_pass@localhost:5432/devconnect_db?schema=public"

# Secret used to sign JWTs (generate your own for real projects)
JWT_SECRET="change-me-in-production"

# Optional: override token expiry (falls back to 1h)
# JWT_EXPIRES_IN=1h
